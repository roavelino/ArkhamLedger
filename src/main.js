const statusTag = document.getElementById('statusTag');
if (statusTag) {
  statusTag.textContent = 'Backend Supabase pronto';
}
console.info('Arkham Ledger frontend loaded. Supabase service modules are available in /src.');
